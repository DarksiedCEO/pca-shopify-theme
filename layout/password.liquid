<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ shop.name }}</title>
  {{ content_for_header }}
  <style>
    body { margin: 0; background: #070707; color: #f5f0e8; font-family: "DM Sans", system-ui, sans-serif; display: flex; align-items: center; justify-content: center; min-height: 100vh; }
  </style>
</head>
<body>
  {{ content_for_layout }}
</body>
</html>
